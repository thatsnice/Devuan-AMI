# Packages installed during debootstrap
# One package per line, blank lines and # comments ignored

# Kernel and bootloader
linux-image-cloud-amd64
grub-pc

# Cloud integration
cloud-init
python3-cffi-backend

# Networking
ifupdown
iproute2
isc-dhcp-client

# SSH access
openssh-server
systemd-standalone-sysusers

# User management
sudo
adduser
passwd
